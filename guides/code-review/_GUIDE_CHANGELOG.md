# 指南体系变更日志（合并）

> 合并 `CODE_ANALYSIS_GUIDE.md`（代码理解）与 `CODE_REVIEW_GUIDE.md`（代码审查）两份指南的版本演进记录，便于在同一处追溯整个指南体系的变更。版本规则见各指引文件头部。
> 下表按指南拆分为**两个独立表格**（分析 / 审查），各表内部按**日期 + 版本**升序排列；两份指南的版本号各自独立，互不可比。
> 注：分析指南 v1.5.0 对应 `deepseek-harness` 项目（2026-08-17）的实践迭代，应用于该项目内的指南副本；本仓库 canonical 副本的版本以 `CODE_ANALYSIS_GUIDE.md` 头部为准。

## 分析指南（CODE_ANALYSIS_GUIDE）

| 版本 | 日期 | 变更摘要 | 来源项目 |
|---|---|---|---|
| v1.0.0 | 2026-08-13 | 初版：四部分结构（使用说明 / Agent 执行指令 / 单文件输出模板 / 附录），覆盖 Python、Java、JS/TS、Node.js、Go、Vue、React 专项指引 | 需求创建 |
| v1.1.0 | 2026-08-13 | 新增阶段零"项目画像与规则自适应"（2.0 节）与第五部分"指引自身的优化迭代机制"（三层文件结构、迭代闭环、变更日志）；产出目录新增 `_PROJECT_PROFILE.md` 与 `_ITERATION_PROPOSAL.md`；自检清单同步更新 | 指引自检 |
| v1.2.0 | 2026-08-13 | 新增代码块切分规则（2.2 节，5–30 行/块、关键行逐行注释、逐行模式）；新增代码功能判定机制（2.3 节，8 类功能类别信号表）；新增跨文件解释能力（2.3 节，被调函数一级内联展开、深度限制、循环保护、摘要卡复用）；输出模板第 5 节重构为"逐代码块四要素"结构（注释代码+块逻辑图+文本解说+跨文件摘要卡）；2.6 增加逐块规模控制 | 指引自检 |
| v1.3.0 | 2026-08-13 | 修复 P0/P1：模板嵌套围栏改用四反引号外层（消除零宽空格隐患）；新增 1.4 节三档分析模式（概览/标准/逐行）；新增 1.5 节全量/增量策略（内容哈希比对、笔记区保护）；模板新增第 8 节"笔记区"；新增 2.7 节"阶段五：产物校对"与第六部分"生成产物校对机制"（10 维度校对清单、行号抽查自证、Mermaid 自检与降级、`_AUDIT_REPORT.md` 报告格式、重试上限）；1.1 节新增最小可用提示词 | 指引自检 + 用户需求 |
| v1.3.1 | 2026-08-13 | 头部新增"分工说明"：与 `CODE_REVIEW_GUIDE.md` 的职责边界（本指南不出具合入结论，审查指南不生成分析文档），共享 `_PROJECT_PROFILE.md` 与覆盖层 | 用户需求（建立独立审查指南） |
| v1.3.2 | 2026-08-14 | 修复内部矛盾与流程缺口（指南审查）：1.4 节"7 节"更正为"8 节"并补第 8 节；版本规则补充"新增章节"亦触发次版本号 +1；1.3 节明确画像/索引固定输出到项目根目录（供审查指南固定路径加载）；1.5 节新增依赖图持久化规则；2.1.6 节新增确认合并规则；2.7 节新增校对规模控制（证据类维度 ≥20% 抽样、不足时全查）；6.2 节行号/调用关系抽查改为"不足时全查" | 指南审查（platform-server） |
| v1.4.0 | 2026-08-14 | 新增 C/C++ 语言专项（用户需求）：2.1 语言识别表新增 `.c` / `.h` / `.cpp` / `.cc` / `.cxx` / `.hpp` 等扩展名判定；2.2 新增"C / C++"解析要点（头文件与实现分离、包含依赖、宏与条件编译标注推测、内存/资源生命周期、C++ 特有、并发边界、动态调用、构建系统识别）；2.1 框架版本确认补充 C/C++ 构建配置（语言标准/编译器/依赖库）；2.1.5 排除规则补充编译产物（`.o`/`.so`/`.dll`/`.exe` 等） | 用户需求（platform-server） |
| v1.5.0 | 2026-08-17 | 修订第三部分 §3「结构清单」类型列：新增「插件(apply)」「服务(Service)」，避免 Cordis 等插件化工程的插件入口 / 服务类被笼统归入「函数」。修订 2.6：明确超大文件（1000–3000 行）的「逐块四要素（①注释代码 ②块逻辑图 ③文本解说 ④跨文件摘要卡）」仅需对所选关键函数（导出类 / 插件入口 + 注册 / 装配 / 生命周期）的关键块执行，其余函数仅在结构清单列出、不逐块展开。 | deepseek-harness |
| v1.5.1 | 2026-08-27 | 修正产出布局（用户需求）：单文件分析文档由"平铺在 `docs/code-analysis/` 根目录（`<文件名>.md`，同名冲突追加父目录名）"改为"按源码相对项目根的目录层级嵌套存放（`docs/code-analysis/<相对路径>/<文件名>.md`）"，使每份 md 落在代码所在文件夹对应的位置，而非全部集中在一个目录；同步更新 1.3 产出位置树与固定位置说明、2.4 生成步骤 1/3/4、第三部分模板跨文件摘要卡示例链接、2.5 互链相对路径约束；README 产出物说明同步 | 用户需求 |
| v1.6.0 | 2026-09-11 | 采纳 `platform-server` 项目 16 条迭代建议中的 13 条通用条目（105 文件全量分析 + 阶段五校对实践）。**§1.5** 新增「产物命名迁移」操作规程（映射建立 / 重复文档决策 / 链重写 / 全局校验 / 口径经验值）；**§2.1** 新增「缺失·需更新·可跳过三态自检」（强制，防止从未分析过的文件被 mtime 增量永久跳过）与多命名匹配注意；**§2.2 Node.js 专项**新增「鉴权白名单与路由豁免的方法粒度」「多 WebSocket 服务共用 http.Server 的 noServer 分发」「ORM 关联识别（关联集中在装配文件的核查）」「响应语义与 HTTP 状态码解耦」「同构多链路差异矩阵」；**§2.2 Python 专项**新增「Web 服务入口契约（多入参分支与向后兼容）」与「跨语言边界协议契约」；**§2.3** 新增「孤立模块识别（强制）」与「跨文件符号存在性校验（强制）」；**§2.6** 新增「同质化文件组差异表要求（强制）」与「按同质化程度分批」；**§2.7** 新增「块规模脚本化检测（附脚本模板）」与「跨文件符号存在性校验」两个校对步骤、Mermaid 自检剔除两类假阳性、修复后维度复检要求；**第六部分**新增 6.2.1「取证上下文判定规则」（剔除行号越界 / Mermaid 节点 ID / 定义行与调用行三类假阳性，附实证数据）；**第三部分模板** §6/§7 补充孤立模块、符号存在性、同构链路差异的固定检查项；**执行确认清单**同步扩充 6 项。另：「ffmpeg 多路输出链」「规则豁免清单」「响应语义解耦」等本项目专属细化内容写入项目覆盖层 `CODE_ANALYSIS_GUIDE.local.md` | platform-server |

## 审查指南（CODE_REVIEW_GUIDE）

| 版本 | 日期 | 变更摘要 | 来源项目 |
|---|---|---|---|
| v1.0.0 | 2026-08-13 | 初版：diff 驱动的审查流程（加载共享画像 → 变更集基线 → 影响面分析 → 七维度检查 → 分级意见与合入结论 → 意见校对）；三档严格度；六要素意见格式；误报控制五条；与 `CODE_ANALYSIS_GUIDE` 的分工与协作接口（共享 `_PROJECT_PROFILE.md` 与覆盖层） | 需求创建 |
| v1.0.1 | 2026-08-14 | 修复内部矛盾与流程缺口（指南审查）：模板示例判级与附录 A 对齐（空引用高频必现→Blocker，新增判级口径说明）；1.4 标准审查"Blocker–Minor 全报"与 2.5.4 噪音上限冲突修正（改为 Major 及以上全报、Minor 聚合）；2.1 新增 diff 命令速查、防错处理（冲突标记/二进制）与分批策略；2.4.3 新增复检闭环（执行者/范围/时限，超时降级打回）；1.3 报告文件名范围标识净化规则；模板第 3 节按严重级别分组小节（3.1–3.5） | 指南审查（platform-server） |
| v1.1.0 | 2026-08-14 | 新增 C/C++ 审查专项（用户需求）：附录 C 速查新增 C 与 C++ 条目（内存生命周期/缓冲区/指针/整数/资源/字符串/并发/C++ 专属要点）；新增附录 D"C/C++ 内存安全审查专项清单"（10 类检查项：内存生命周期、指针安全、缓冲区、整数与算术、资源管理、字符串、并发、C++ 对象生命周期/异常安全/模板与转换） | 用户需求（platform-server） |
| v1.1.1 | 2026-08-27 | 协作说明补充：分析文档已按源码目录层级嵌套存放（见分析指南 v1.5.1），5.1.2 节新增定位方式（按"源文件相对项目根的路径"拼接或经 `_INDEX.md` 的"分析文档链接"列检索），避免误假设分析文档平铺在 `docs/code-analysis/` 根目录 | 用户需求 |
